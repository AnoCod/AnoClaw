appId: com.anoclaw.app
productName: AnoClaw
# Use the locally cached/unpacked Electron build so packaging never
# re-downloads electron from GitHub. Machine-specific: adjust the path on
# other machines, or remove this line to let electron-builder fetch its own.
electronDist: C:/Users/Administrator/AppData/Local/electron/Cache/electron-v42.4.0-win32-x64
directories:
  output: release9
files:
  - dist/**/*
  - "!dist/**/*.map"
  - "!dist/electron/bridge.js.map"
  - src/public/**/*
  - "!src/public/ts/**"
  - "!src/public/tsconfig.json"
  - "!src/public/js/**/*.js.map"
  - src/electron/setup-wizard.html
  - docs/**/*
  - "!docs/design-md/**"
  - "!docs/待修复问题点"
  - plugins/**/*
  - plugins/**/node_modules/**/*
  - "!**/AGENTS.md"
  - "!**/CLAUDE.md"
  - "!**/CLAUDE.*.md"
  - "!plugins/**/data/**"
  - "!plugins/**/logs/**"
  - "!plugins/**/tmp/**"
  - "!plugins/**/cache/**"
  - skills/**/*
  - node_modules/**/*
  - "!**/*.map"
  - "!node_modules/.cache"
  - "!node_modules/@types"
asar: true
asarUnpack:
  # dist/** — fully unpacked: Worker threads use ESM imports which resolve
  # via real filesystem. Electron's asar-patched require() doesn't cover ESM.
  - "dist/**"
  # node_modules/** — fully unpacked: Worker thread code transitively imports
  # core modules (e.g. PluginAPI → SessionManager → pino/ws/zod). ESM imports
  # from Worker context cannot read from asar.
  - "node_modules/**"
  - "!node_modules/.cache"
  - "!node_modules/@types"
  # User-writable / agent-readable directories
  - "plugins/**"
  - "plugins/**/node_modules/**"
  - "!plugins/**/data/**"
  - "!plugins/**/logs/**"
  - "!plugins/**/tmp/**"
  - "!plugins/**/cache/**"
  - "skills/**"
  - "docs/**"
extraResources:
  - from: plugins
    to: app.asar.unpacked/plugins
    filter:
      - "**/node_modules/**/*"
      - "!**/*.map"
win:
  target:
    - target: nsis
      arch: [x64]
  icon: build/icon.ico
nsis:
  oneClick: false
  allowToChangeInstallationDirectory: true
  createDesktopShortcut: true
  createStartMenuShortcut: true
  shortcutName: AnoClaw
  installerIcon: build/icon.ico
  installerHeaderIcon: build/icon.ico
  uninstallerIcon: build/icon.ico
extraMetadata:
  main: dist/electron/bridge.cjs
